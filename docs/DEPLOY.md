# Deployment

For production deployment on Windows:
- Create a Python virtualenv with Python 3.12
- Install system dependencies (Visual C++ redistributable, audio drivers)
- Install requirements and configure .env
- Run `python -m jarvis.cli` or bundle with PyInstaller
