# Installation and deployment notes

This document contains notes for building and deploying JARVIS on development and production.

- Use Python 3.12
- Ensure native dependencies (sounddevice, pocketsphinx, webrtcvad, edge-tts) are installed on the target platform
- To create a single-file executable, use PyInstaller (see scripts/build_release.*)

