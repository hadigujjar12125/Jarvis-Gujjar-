# Installer and build helper

This script builds a cross-platform distribution and creates a local installer using PyInstaller.
Requires PyInstaller installed in the environment.

Usage (Unix):
    ./scripts/build_release.sh

Windows (PowerShell):
    .\scripts\build_release.bat
